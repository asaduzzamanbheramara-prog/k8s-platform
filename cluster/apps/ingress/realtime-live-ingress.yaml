apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: realtime-live-ingress
  namespace: argocd
spec:
  ingressClassName: traefik
  rules:
  - host: live.shopnoltd.dpdns.org
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: realtime-live
            port:
              number: 80
