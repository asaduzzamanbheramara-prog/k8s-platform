apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kc-ingress
  namespace: argocd
spec:
  ingressClassName: traefik
  rules:
  - host: kc.shopnoltd.dpdns.org
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: kc
            port:
              number: 80
