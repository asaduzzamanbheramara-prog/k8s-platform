apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: test2
  namespace: default
spec:
  ingressClassName: traefik

  tls:
  - secretName: wildcard-shopnoltd
    hosts:
    - test2.shopnoltd.dpdns.org

  rules:

  - host: test2.shopnoltd.dpdns.org
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: chat-ui
            port:
              number: 80

