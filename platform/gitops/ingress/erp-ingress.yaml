apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: erp-ingress
  namespace: argocd
spec:
  ingressClassName: traefik
  rules:
  - host: erp.shopnoltd.dpdns.org
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: erp-service
            port:
              number: 80
