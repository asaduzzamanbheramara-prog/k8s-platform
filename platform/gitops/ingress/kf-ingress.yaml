apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kf-ingress
  namespace: argocd
spec:
  ingressClassName: traefik
  rules:
  - host: kf.shopnoltd.dpdns.org
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: kf-service
            port:
              number: 80
