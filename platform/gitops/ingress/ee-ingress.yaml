apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ee-ingress
  namespace: argocd
spec:
  ingressClassName: traefik
  rules:
  - host: ee.shopnoltd.dpdns.org
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ee-service
            port:
              number: 80
