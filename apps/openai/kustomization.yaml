apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: openai

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - configmap.yaml
  - secrets.yaml

labels:
  - pairs:
      app: cursor-ai-system

commonAnnotations:
  system: "cursor-like-ai-platform"
