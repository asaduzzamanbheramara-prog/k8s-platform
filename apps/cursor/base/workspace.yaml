apiVersion: apps/v1
kind: Deployment
metadata:
  name: workspace
  namespace: cursor
spec:
  replicas: 1
  selector:
    matchLabels:
      app: workspace
  template:
    metadata:
      labels:
        app: workspace
    spec:
      containers:
        - name: workspace
          image: nginx:alpine
