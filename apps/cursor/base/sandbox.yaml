apiVersion: apps/v1
kind: Deployment
metadata:
  name: sandbox
  namespace: cursor
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sandbox
  template:
    metadata:
      labels:
        app: sandbox
    spec:
      containers:
        - name: sandbox
          image: nginx:alpine
