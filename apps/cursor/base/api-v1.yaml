apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-v1
  namespace: cursor
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-v1
  template:
    metadata:
      labels:
        app: api-v1
    spec:
      containers:
        - name: api
          image: nginx:alpine
          ports:
            - containerPort: 80
