apiVersion: v1
kind: Service

metadata:
  name: postfix
  namespace: mail

spec:
  type: LoadBalancer

  selector:
    app: postfix

  ports:
    - name: smtp
      protocol: TCP
      port: 25
      targetPort: 25

    - name: submission
      protocol: TCP
      port: 587
      targetPort: 587
